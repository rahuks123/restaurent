  <%= render partial: "menu_items-section", locals: {
    section_title: "starter",
   menuitems: MenuItem.starter,
  } %>

  <%= render partial: "menu_items-section", locals: {
    section_title: "breakfast",
    menuitems: MenuItem.breakfast,
   } %>

  <%= render partial: "menu_items-section", locals: {
    section_title: "lunch",
    menuitems: MenuItem.lunch,
  } %>

  <%= render partial: "menu_items-section", locals: {
    section_title: "dinner",
    menuitems: MenuItem.dinner,
  } %>

  <%= render partial: "menu_items-section", locals: {
    section_title: "dessert",
    menuitems: MenuItem.dessert,
  } %>
